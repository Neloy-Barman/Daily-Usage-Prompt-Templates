# Code Files to README.md
``````md
## Context
I have `<context>` (the project description). Now I want an easily understandable `README.md` file describing the whole project. It should contain the intent, overview, workflow implementation, and usage of code components so that, anyone can go through the documentation and get the whole concept of the code files.

## Code Files
```
<code_file_data>
```

## Task
Provide me with a `README` documentation in `.md` format.

## Rules
- Provide with a suitable and the most relevant name for the markdown file. Include the filename at the top of the Markdown content.
- The contents should be in a professional format and act as a documentation so that anyone can go through it and get the concepts and stuff mentioned.
- Describe proper workflow implementation.
- Break down the code into its key components.
- Describe and annotate code components clearly, such as by including code snippets with explanations.
- Use Markdown elements as needed, such as `headings`, `bold text`, `lists`, and others.
- In `lists` and `notes`, bold `key terms` or `labels` that appear before a colon.
- The sentences should be `Concise`, `Easy`, `Clean` and in `Beginner-friendly` tone.
- The content should be `To the point`.
- Return me with the whole response as a Markdown within **4 Backticks**(````<generated_readme_markdown_content>````).
- Strictly keep the generated content within **4 backticks**.

## Response Return Format
````md
<generated_readme_markdown_content>
````
``````

# Expected Response from LLMs & Platforms

- **LLM**: `gpt-5-high-new-system-prompt`<BR>**Platform**: `https://lmarena.ai/`

